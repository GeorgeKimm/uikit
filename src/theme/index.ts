// Theme configuration (recommended for customization)
export { uiKitThemeOptions } from './themeOptions';

// Ready-to-use theme (optional, for extending)
export { defaultTheme } from './defaultTheme';

// Theme provider (recommended for quick start)
export { UiKitThemeProvider } from './ThemeProvider';
