# 📦 Руководство по публикации UI-Kit библиотеки в NPM

## 📋 Содержание
1. [Подготовка к публикации](#подготовка-к-публикации)
2. [Настройка имени пакета](#настройка-имени-пакета)
3. [Версионирование](#версионирование)
4. [Процесс публикации](#процесс-публикации)
5. [После публикации](#после-публикации)

---

## 🎯 Подготовка к публикации

### Шаг 1: Регистрация в NPM

Если у вас еще нет аккаунта на npmjs.com:

1. Зарегистрируйтесь на [npmjs.com](https://www.npmjs.com/signup)
2. Подтвердите email
3. Войдите в систему через терминал:
   ```bash
   npm login
   ```
   Введите username, password и email

### Шаг 2: Проверка текущей конфигурации

Убедитесь, что проект собирается без ошибок:

```bash
# Установите зависимости (если еще не установлены)
npm install

# Проверьте линтинг
npm run lint

# Соберите проект
npm run build
```

После успешной сборки в папке `dist/` должны появиться:
- `ui-kit.es.js` (ESM формат)
- `ui-kit.umd.js` (UMD формат)
- `index.d.ts` (TypeScript декларации)
- `style.css` (если есть стили)

---

## 🏷️ Настройка имени пакета

### ⚠️ ВАЖНО: Имя пакета должно быть уникальным!

Ваше текущее имя: `@george-kunka/ui-kit`

### Варианты именования:

#### 1️⃣ **Scoped пакет (рекомендуется)** 
Формат: `@username/package-name` или `@organization/package-name`

**Преимущества:**
- Меньше вероятность конфликта имен
- Можно использовать более простые названия
- Выглядит профессионально
- Бесплатно для публичных пакетов

**Примеры:**
```json
"name": "@george-kunka/ui-kit"
"name": "@georgekimm/react-ui-components"
"name": "@mycompany/design-system"
```

#### 2️⃣ **Обычный пакет**
Формат: `package-name`

**Требования:**
- Должно быть глобально уникальным
- Только строчные буквы
- Можно использовать дефисы и цифры

**Примеры:**
```json
"name": "georgekimm-ui-kit"
"name": "gk-react-components"
```

### Как проверить доступность имени:

```bash
# Для scoped пакета
npm view @george-kunka/ui-kit

# Для обычного пакета
npm view georgekimm-ui-kit
```

Если вы видите `npm error code E404` - имя свободно! ✅

---

## 📝 Где менять имя пакета

### 1. `package.json` (ОБЯЗАТЕЛЬНО)

```json
{
  "name": "@george-kunka/ui-kit",  // ← ИЗМЕНИТЕ ЗДЕСЬ
  "version": "0.0.1",
  // ...
}
```

### 2. `vite.config.ts` (ОПЦИОНАЛЬНО, но рекомендуется)

В `vite.config.ts` есть два места, где используется имя:

```typescript
build: {
  lib: {
    entry: resolve(__dirname, 'src/index.ts'),
    name: 'UiKit',  // ← Это имя для UMD глобальной переменной
    fileName: (format) => `ui-kit.${format}.js`,  // ← Имя файлов
    formats: ['es', 'umd'],
  },
  // ...
}
```

**Объяснение:**
- `name: 'UiKit'` - это имя **глобальной переменной** для UMD формата (когда библиотека подключается через `<script>` тег). Можно оставить как есть или изменить на `GeorgeKimmUiKit`
- `fileName` - имя выходных файлов. Можно оставить `ui-kit` или изменить на что-то более уникальное

**Рекомендация:** Если меняете имя в `package.json` на `@george-kunka/ui-kit`, можете:
- Оставить `name: 'UiKit'` (для простоты)
- Или изменить на `name: 'GeorgeKimmUiKit'` (для уникальности)

### 3. `README.md` (ОБЯЗАТЕЛЬНО)

Замените все упоминания `@george-kunka/ui-kit` на ваше новое имя:

```markdown
# @george-kunka/ui-kit

## Установка
npm install @george-kunka/ui-kit
```

### 4. `AGENT_CONTEXT.md` (ОПЦИОНАЛЬНО)

Обновите упоминание имени пакета для консистентности.

---

## 🔢 Версионирование (Semantic Versioning)

### Формат версии: `MAJOR.MINOR.PATCH`

Пример: `1.2.3`
- **MAJOR (1)** - несовместимые изменения API
- **MINOR (2)** - новая функциональность (обратно совместимая)
- **PATCH (3)** - исправления багов

### Правила версионирования:

#### Для первой публикации:
```json
"version": "0.1.0"  // Рекомендуется для начала
```
или
```json
"version": "1.0.0"  // Если библиотека готова к production
```

#### Для последующих обновлений:

**Патч (исправление багов):**
```bash
npm version patch  # 0.1.0 → 0.1.1
```

**Минор (новая функциональность):**
```bash
npm version minor  # 0.1.1 → 0.2.0
```

**Мажор (breaking changes):**
```bash
npm version major  # 0.2.0 → 1.0.0
```

### Команда `npm version` автоматически:
1. Обновляет версию в `package.json`
2. Создает git commit
3. Создает git tag

---

## 🚀 Процесс публикации

### Полный чек-лист перед публикацией:

```bash
# 1. Убедитесь, что вы залогинены
npm whoami

# 2. Проверьте, что имя пакета уникально
npm view @george-kunka/ui-kit

# 3. Очистите старую сборку (если нужно)
rm -rf dist

# 4. Установите зависимости
npm install

# 5. Проверьте линтинг
npm run lint

# 6. Соберите проект
npm run build

# 7. Проверьте содержимое пакета (что будет опубликовано)
npm pack --dry-run

# 8. (Опционально) Протестируйте локально
npm pack
# Это создаст файл georgekimm-ui-kit-0.1.0.tgz
# Вы можете установить его в тестовом проекте:
# npm install /path/to/georgekimm-ui-kit-0.1.0.tgz
```

### Публикация:

#### Для scoped пакета (публичного):
```bash
npm publish --access public
```

#### Для обычного пакета:
```bash
npm publish
```

### Что происходит при публикации:

1. NPM упаковывает файлы из папки `dist/` (указано в `"files": ["dist"]`)
2. Загружает пакет в реестр npm
3. Пакет становится доступен по адресу: `https://www.npmjs.com/package/@george-kunka/ui-kit`

---

## ✅ После публикации

### 1. Проверьте публикацию

Откройте в браузере:
```
https://www.npmjs.com/package/@george-kunka/ui-kit
```

### 2. Протестируйте установку

Создайте новый тестовый проект:
```bash
mkdir test-ui-kit
cd test-ui-kit
npm init -y
npm install @george-kunka/ui-kit react react-dom @mui/material @emotion/react @emotion/styled
```

### 3. Создайте git tag (если не использовали `npm version`)

```bash
git tag v0.1.0
git push origin v0.1.0
```

### 4. Обновите README.md

Добавьте бейдж с версией:
```markdown
[![npm version](https://badge.fury.io/js/@georgekimm%2Fui-kit.svg)](https://www.npmjs.com/package/@george-kunka/ui-kit)
```

---

## 🔄 Обновление опубликованного пакета

### Процесс обновления:

```bash
# 1. Внесите изменения в код
# 2. Соберите проект
npm run build

# 3. Обновите версию (автоматически создаст commit и tag)
npm version patch  # или minor, или major

# 4. Опубликуйте новую версию
npm publish --access public

# 5. Отправьте изменения в git
git push && git push --tags
```

---

## 🛡️ Важные моменты

### ✅ Что включается в публикацию:
- Только папка `dist/` (указано в `"files": ["dist"]`)
- `package.json`
- `README.md`
- `LICENSE` (если есть)

### ❌ Что НЕ включается:
- `node_modules/`
- `src/` (исходники)
- `.storybook/`
- Файлы из `.npmignore` или `.gitignore`

### 🔐 Безопасность:
- Никогда не публикуйте приватные ключи или токены
- Проверьте `.npmignore` перед публикацией
- Используйте `npm pack --dry-run` для предварительного просмотра

### 📊 Мониторинг:
- Следите за загрузками на npmjs.com
- Отвечайте на issues в GitHub
- Поддерживайте актуальную документацию

---

## 📚 Дополнительные ресурсы

- [NPM Documentation](https://docs.npmjs.com/)
- [Semantic Versioning](https://semver.org/)
- [Publishing Scoped Packages](https://docs.npmjs.com/creating-and-publishing-scoped-public-packages)

---

## 🎯 Краткая шпаргалка

```bash
# Первая публикация
npm login
npm publish --access public

# Обновление патча (0.1.0 → 0.1.1)
npm version patch && npm publish --access public

# Обновление минора (0.1.1 → 0.2.0)
npm version minor && npm publish --access public

# Обновление мажора (0.2.0 → 1.0.0)
npm version major && npm publish --access public

# Отменить публикацию (только в течение 72 часов!)
npm unpublish @george-kunka/ui-kit@0.1.0

# Пометить версию как устаревшую
npm deprecate @george-kunka/ui-kit@0.1.0 "Please use version 0.2.0"
```

---

**Удачи с публикацией! 🚀**
